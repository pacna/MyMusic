import { Button, styled } from "@mui/material";
import { Color } from "../types/local";

export const StyledPrimaryButton = styled(Button)({
    color: Color.BlueMarguerite,
    borderColor: Color.BlueMarguerite,
});
